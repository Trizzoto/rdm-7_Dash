{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rdm7.dev/schema/widgets.schema.meta.json",
  "title": "RDM-7 Widget Schema (meta-validator)",
  "description": "Validates schema/widgets.schema.json. Catches missing required keys, wrong types, unknown field-type values, malformed colour strings.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "widgets"],
  "properties": {
    "schema_version": {
      "type": "integer",
      "minimum": 1
    },
    "comment": { "type": "string" },
    "widgets": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/widget" }
    }
  },
  "$defs": {
    "widget": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "display_name", "default_size", "constraints", "fields"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$",
          "description": "Widget type identifier (snake_case)."
        },
        "display_name": { "type": "string", "minLength": 1 },
        "singleton": { "type": "boolean" },
        "hidden": {
          "type": "boolean",
          "description": "When true, codegen emits hidden:true and the web editor omits this type from the add-widget palette. The type stays fully registered — existing layouts using it still load and edit — it just cannot be added anew. Used for experimental widgets not yet ready for users."
        },
        "default_size": {
          "type": "object",
          "additionalProperties": false,
          "required": ["w", "h"],
          "properties": {
            "w": { "type": "integer", "minimum": 1 },
            "h": { "type": "integer", "minimum": 1 }
          }
        },
        "default_position": {
          "oneOf": [
            { "type": "null" },
            {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "x": { "type": "integer" },
                "y": { "type": "integer" }
              }
            }
          ]
        },
        "constraints": {
          "type": "object",
          "additionalProperties": false,
          "required": ["min_w", "min_h", "max_w", "max_h"],
          "properties": {
            "min_w": { "$ref": "#/$defs/size_value" },
            "min_h": { "$ref": "#/$defs/size_value" },
            "max_w": { "$ref": "#/$defs/size_value" },
            "max_h": { "$ref": "#/$defs/size_value" }
          }
        },
        "_doc": { "type": "string" },
        "fields": {
          "type": "array",
          "items": { "$ref": "#/$defs/field" }
        }
      }
    },
    "size_value": {
      "description": "Either an integer pixel value, or one of the screen-size tokens that expand at codegen time.",
      "oneOf": [
        { "type": "integer" },
        {
          "type": "string",
          "enum": ["screen_w", "screen_h", "screen_origin_x", "screen_origin_y"]
        }
      ]
    },
    "field": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "label", "type", "default"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$"
        },
        "label": { "type": "string", "minLength": 1 },
        "type": {
          "type": "string",
          "enum": [
            "text",
            "textarea",
            "number",
            "stepper",
            "stepper-auto",
            "slider",
            "color",
            "checkbox",
            "select",
            "font",
            "image_picker",
            "can_id",
            "gradient_stops",
            "anim_frames"
          ]
        },
        "default": {
          "description": "Field default. Colour fields use '#RRGGBB' string; everything else uses the natural JSON type."
        },
        "category": {
          "type": "string",
          "enum": ["data", "appearance", "alerts", "thresholds"]
        },
        "group": { "type": "string" },
        "help": {
          "type": "string",
          "description": "Inspector tooltip text shown on the per-field ⓘ icon; emitted by codegen_widget_defs into WIDGET_DEFS."
        },
        "enabled_by": {
          "type": "string",
          "description": "Gate this field's visibility on another field. Forms: 'field' (show when truthy), '!field' (show when falsy), 'field=a,b' (show when value is one of a/b — gate by a select). Combine conditions with '&' for AND, e.g. 'show_ticks&major_tick_image_name' or 'show_needle&!needle_image_name'. The web editor hides the field when the condition is unmet."
        },
        "inline": { "type": "string" },
        "rule_only": {
          "type": "boolean",
          "description": "Field is offered as a conditional-rule target only, not a static setting — codegen emits ruleOnly and both inspectors skip it in the normal field loop."
        },
        "night_overridable": { "type": "boolean" },
        "night_key": {
          "type": "string",
          "description": "Optional remap of the night-override storage key (e.g. warning's border_color_style is stored under 'border_color')."
        },
        "min": { "type": "number" },
        "max": { "type": "number" },
        "step": { "type": "number" },
        "options": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["value", "label"],
            "properties": {
              "value": {
                "oneOf": [
                  { "type": "string" },
                  { "type": "number" },
                  { "type": "boolean" }
                ]
              },
              "label": { "type": "string" }
            }
          }
        },
        "_doc": { "type": "string" },
        "_raw_options": {
          "type": "string",
          "description": "Raw JS expression to use as the options literal (e.g. an external constant name like '_METER_POSITION_OPTIONS' or 'Array.from(...)'). Mutually exclusive with 'options'."
        },
        "_raw_extra": {
          "type": "object",
          "description": "Extra JS-only fields (e.g. autoFn callback). Each value is a raw JS expression string emitted verbatim.",
          "additionalProperties": { "type": "string" }
        }
      },
      "allOf": [
        {
          "description": "Color field defaults must be '#RRGGBB' strings.",
          "if": { "properties": { "type": { "const": "color" } } },
          "then": {
            "properties": {
              "default": {
                "type": "string",
                "pattern": "^#[0-9A-Fa-f]{6}$"
              }
            }
          }
        },
        {
          "description": "Select fields must have either 'options' or '_raw_options', not both, not neither.",
          "if": { "properties": { "type": { "const": "select" } } },
          "then": {
            "oneOf": [
              { "required": ["options"], "not": { "required": ["_raw_options"] } },
              { "required": ["_raw_options"], "not": { "required": ["options"] } }
            ]
          }
        },
        {
          "description": "Stepper / slider must have min and max.",
          "if": {
            "properties": {
              "type": { "enum": ["stepper", "slider", "stepper-auto"] }
            }
          },
          "then": { "required": ["min", "max"] }
        }
      ]
    }
  }
}
