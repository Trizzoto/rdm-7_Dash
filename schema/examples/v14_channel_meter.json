{
  "_comment": "v14 channel-binding smoke test — single meter bound to the canonical 'rpm' channel.",
  "_purpose": "Confirms widget_meter loads via channel binding, pulls min/max/threshold from /lfs/channels.json, and renders with live signal updates.",
  "_install": "Save as /lfs/layouts/v14_test.json on the dash, then activate via web UI Layout dropdown. Or POST to /api/layout/save?apply=1.",
  "schema_version": 14,
  "name": "v14 Channel Test",
  "signals": [
    {
      "name": "RPM",
      "can_id": "0x360",
      "bit_start": 0,
      "bit_length": 16,
      "scale": 1.0,
      "offset": 0.0,
      "endian": 1,
      "unit": "rpm"
    }
  ],
  "screens": [
    {
      "widgets": [
        {
          "id": "meter_rpm",
          "type": "meter",
          "x": 0,
          "y": 0,
          "w": 280,
          "h": 280,
          "config": {
            "slot": 9,
            "channel": "rpm",
            "start_angle": 135,
            "end_angle": 45,
            "minor_tick_count": 21,
            "major_tick_every": 5,
            "needle_width": 4,
            "needle_color": 16777215,
            "needle_tip_style": 2,
            "shadow_enabled": true,
            "shadow_dynamic": true,
            "tick_label_divisor": 1000
          }
        }
      ]
    }
  ]
}
