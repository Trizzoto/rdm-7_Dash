# widgets/ — Phase 0F widget modules.
# Source files are registered directly in main/CMakeLists.txt SRCS.
# This file exists so the directory is recognised as a source subtree and
# future source additions only require editing main/CMakeLists.txt.
