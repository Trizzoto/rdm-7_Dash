# web/ — Phase 4: companion web tool served over WiFi.
# Planned modules: web_layout_api.c (REST endpoints for layout CRUD),
#                  web_assets.c     (embedded HTML/JS/CSS served from LittleFS).
# Source files will be added to main/CMakeLists.txt SRCS when implemented.
