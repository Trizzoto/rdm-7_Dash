# design_mode/ — Phase 3: on-device drag-and-drop layout editor.
# Planned modules: design_overlay.c (drag handles, resize, snapping),
#                  design_toolbar.c (widget palette, save/load buttons).
# Source files will be added to main/CMakeLists.txt SRCS when implemented.
