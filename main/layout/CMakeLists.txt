# layout/ — Phase 2: LittleFS-backed layout persistence.
# Planned modules: layout_store.c (JSON serialise/deserialise widget positions),
#                  layout_manager.c (named layouts, load/save/delete).
# Source files will be added to main/CMakeLists.txt SRCS when implemented.
